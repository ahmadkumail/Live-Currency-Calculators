export const siteConfig = {
    name: "Live Currency Converter",
    description: "Convert currencies with live exchange rates from across the globe.",
};

export type SiteConfig = typeof siteConfig;
